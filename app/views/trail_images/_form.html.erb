<%= form_with(model: trail_image) do |form| %>
  <% if trail_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trail_image.errors.count, "error") %> prohibited this trail_image from being saved:</h2>

      <ul>
        <% trail_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :trail_id, style: "display: block" %>
    <%= form.text_field :trail_id %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
